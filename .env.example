# Anthropic
ANTHROPIC_API_KEY=your_api_key_here

# OpenAI
OPENAI_API_KEY=your_api_key_here

# Google Gemini
GOOGLE_GENERATIVE_AI_API_KEY=your_api_key_here

# Postgres — run: docker run --name cartographer-db -e POSTGRES_PASSWORD=postgres -p 5432:5432 -d postgres
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/cartographer

# TLS to Postgres. Left unset, it's inferred from the host: off for localhost,
# on for anything else (managed Postgres such as RDS requires it). Set to
# `disable` to force it off, or `require` to force it on.
# PGSSLMODE=

# Max Postgres connections per app instance (default: 5). Keep instances × this
# value under the server's max_connections.
# PG_POOL_MAX=5

# Active product domain — matches a file in /domain/*.config.json
DOMAIN=beauty

# LLM provider: anthropic | openai | google
LLM_PROVIDER=google

# Set to true to skip the LLM entirely and use a scripted demo loop (no API key needed)
MOCK_LLM=false

# Number of products shown per category (default: 4)
PRODUCT_LIMIT=4
